"""Reserved for Sprint 4.

This module will host the scoring + ordering layer that consumes
`CandidateFeatures` and `CandidateExplanation` objects to produce the
final top-100 ranking required by the hackathon submission spec.
Deliberately empty in Sprint 2.
"""
